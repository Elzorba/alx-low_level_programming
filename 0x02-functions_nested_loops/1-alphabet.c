#include "main.h"
/**
 * main : intry point  print alphabet using put char
 * return : if 0 succes
 */
void print_alphabet(void)
{
        int ch
        for (ch = 'a'; ch <= 'z'; ch++)

         _putchar(ch);
         _putchar("\n");
}
        
