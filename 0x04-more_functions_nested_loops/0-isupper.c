#include "main.h"

/**
 * _isupper check if a letter is upper
 *  @c : the number of cheched
 * return 1 for upper letter or 0 for any else
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
	/* return 1 for upper letter and 0 for ent thing else */
	return (1);
	}
	return (0);
}
