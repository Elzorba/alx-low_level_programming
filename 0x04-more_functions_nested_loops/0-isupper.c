#include "main.h"

/**
 * -isupper check if a letter is upper
 *  @x : the number of cheched
 * return 1 for upper letter or 0 for any else
 */

int -isupper(int x)
{
	if (x >=65 && x <=90)
	{
	return (1);
	}
	return (0);
}
